# YO-Trace 完整开发路线图 V2.0

**当前状态：** 0 行代码，刚立项

**目标：** 做出可用的内测版 v1.0.0，上架微软商店

**开发工具：** Red Panda Dev-C++ 6.5（阶段一、二），VSCode + MSVC（阶段三以后）

**更新日期：** 2026年8月26日

---

## 核心设计决策总览

| 决策点      | 选择                         | 理由                       |
| -------- | -------------------------- | ------------------------ |
| 开发工具（早期） | Red Panda Dev-C++ 6.5      | 零配置、开箱即用、支持Windows API   |
| 开发工具（后期） | VSCode + MSVC              | 处理复杂依赖库（OpenSSL、SQLite等） |
| 截图方案     | 480P WebP + 差异帧 + 夜间去重     | 存储量与辨识度的平衡点              |
| 存储分层     | 3个月本地 + 过期云端归档             | 用户访问速度与硬盘空间的权衡           |
| 索引保留     | 永久保留，永不删除                  | 保证用户始终能搜索到历史记录           |
| 触发机制     | 事件驱动 + 0.2秒静默合并            | 避免高频操作导致数据爆炸             |
| AI方案     | 本地部署（Ollama + Qwen2.5:7B）  | 无需实名认证，数据不出本地            |
| 加密方案     | 主密码 + 标签级联派生密钥             | 一套密码，标签级隔离               |
| 分发渠道     | Microsoft Store + VSCode插件 | 零门槛安装，覆盖开发者场景            |

---

## 阶段零：项目启动（第1周）

**目标：** 不做任何开发，先验证方向
**代码量：** 0 行

### 任务 0.1：研究竞品

- [x] 打开 `Windrecorder GitHub` 页面，通读 `README.md`
- [ ] 照着文档安装 `Windrecorder`，完整走一遍流程
- [ ] 使用 `Windrecorder` **至少 3 天**，记录所有卡顿/困惑/不满
- [ ] 打开 `LifeTrace GitHub` 页面，通读 `README.md`
- [ ] 打开 `ScreenMind GitHub` 页面，通读 `README.md`
- [x] 整理成文档：竞品做了什么、没做什么、卡在哪

### 任务 0.2：验证需求

- [ ] 找 5 个**非技术背景**的同事/朋友，问同一个问题：
  
  > "你有没有过明明看过某个东西，但就是找不到在哪里的情况？"

- [ ] 记录每个人的回答

- [ ] 整理出 3 个真实使用场景

### 任务 0.3：明确差异化

- [x] 用一句话写出 YO-Trace 和 Windrecorder 的核心差异
- [x] 用一句话写出目标用户是谁
- [x] 用一句话写出用户为什么选 YO-Trace

**阶段零完成标志：** 确定需求真实存在，且竞品没解决好

---

## 阶段一：窗口采集引擎（第2-3周）

**工具：** Red Panda Dev-C++ 6.5
**目标：** 能枚举所有窗口，记录窗口标题和坐标
**代码量：** ~500 行

### 任务 1.1：搭建开发环境

- [x] 下载安装 Red Panda Dev-C++ 6.5
- [x] 新建 C++控制台项目
- [x] 确认能编译运行 `Hello World`

### 任务 1.2：枚举所有窗口

- [x] `#include <windows.h>`
- [x] 实现 `EnumWindows` 回调函数
- [x] 获取每个窗口的标题（`GetWindowText`）
- [x] 获取每个窗口的坐标（`GetWindowRect`）
- [x] 打印所有窗口信息到控制台

### 任务 1.3：过滤无效窗口

- [x] 过滤掉不可见窗口（`IsWindowVisible`）
- [x] 过滤掉桌面窗口（类名:`Progman, WorkerW`）
- [x] 过滤掉任务栏窗口（类名: `Shell_TrayWnd`）
- [x] 过滤掉无标题窗口（标题为空）

### 任务 1.4：获取窗口详细信息

- [x] 获取窗口所属进程名（GetWindowThreadProcessId + QueryFullProcessImageName）
- [x] 获取窗口类名（GetClassName）
- [x] 获取窗口 Z 序（GetWindow 的 GW_HWNDPREV）
- [x] 获取窗口是否激活（GetForegroundWindow 比较）

### 任务 1.5：定时采集

- [x] 设置定时器，每 5 秒采集一次
- [x] 每次采集时记录当前窗口快照
- [x] 支持手动触发采集（如按 F12）

**阶段一完成标志：** 运行程序，能看到当前所有窗口列表

---

## 阶段二：本地存储层（第3-4周）

**工具：** Red Panda Dev-C++ 6.5
**目标：** 能把窗口快照存进 SQLite
**代码量：** ~800 行

### 任务 2.1：集成 SQLite

- [x] 下载 SQLite3 源码（sqlite3.h + sqlite3.c）
- [x] 将 sqlite3.c 添加到项目中编译
- [x] 写测试：创建数据库，创建表，插入一行数据
- [x] 用 sqlite3 命令行工具查看是否写入成功

### 任务 2.2：设计数据库表结构

- [x] 创建 snapshots 表：id, timestamp
- [x] 创建 windows 表：id, snapshot_id, title, app_name, x1, y1, x2, y2
- [x] 创建 text_blocks 表（预留）

### 任务 2.3：写入数据

- [x] 每次采集时将窗口快照写入数据库
- [x] 使用事务（BEGIN/COMMIT）提高写入性能
- [x] 记录写入耗时，确保 < 50ms

### 任务 2.4：读取数据

- [x] 实现按时间范围查询
- [x] 实现按窗口标题模糊查询
- [x] 写命令行测试，能打印查询结果

### 任务 2.5：去重逻辑（第一版）

- [x] 每次采集前获取上一次的窗口列表
- [x] 计算两次采集的差异
- [x] 如果没有变化，不写入数据库

**阶段二完成标志：** 数据库中能查到窗口记录，且没有重复

---

## 阶段三：文字照片采集（第5-7周）

**工具：** VSCode + MSVC（开始处理复杂依赖）
**目标：** 能识别屏幕上的文字，存入数据库
**代码量：** ~1500 行

### 任务 3.1：开发工具链（保留 MinGW）

- [x] 安装 VSCode
- [x] 安装 C/C++ 插件
- [x] 将阶段一、二的代码迁移到 VSCode 项目（构建仍走 MinGW g++）
- [x] 确认编译通过，原有功能正常
- [x] 确认不切换到 MSVC：用户要求保留 MinGW，Windows.Media.Ocr 通过**手写 WinRT ABI**（`winrt_ocr_abi.h`）调用，无需 C++/WinRT 编译器
- [x] 链接 `-lruntimeobject -lole32` 以解析 RoInitialize / RoGetActivationFactory / CoTaskMem*

### 任务 3.2：Windows OCR API 集成（原生优先 + Tesseract 回退）

> 实现说明：按用户要求**改用 Windows 原生 OCR（Windows.Media.Ocr，WinRT）**作为主引擎，
> 通过手写 ABI（`src/phase3/winrt_ocr_abi.h` + `src/phase3/ocr_windows.cpp`）在 MinGW 下直接调用，
> 识别整屏文字并解析带坐标文字块（Lines→Words→BoundingRect+Text，置信度固定 1.0）。
> **Tesseract 作为回退**：当 Windows 原生引擎不可用（无 Win10/11 OCR、缺语言包等）时，
> 工厂函数 `CreateOcrEngine()` 自动回退到 `CreateTesseractOcrEngine()`（子进程 tesseract.exe）。
> 因沙箱无法运行 WinRT/OCR，能否真正识别需在本机双击 yotrace_phase3.exe 验证（看日志"[OCR] 使用 Windows 原生 OCR"与"文本块=N"的 N>0）。

- [x] 手写 WinRT ABI 配置（接口 GUID、vtable 布局、枚举值，稳定 ABI）
- [x] 实现 WindowsMediaOcrEngine：SoftwareBitmap(Bgra8) -> LockBuffer 写 BGRA -> RecognizeAsync（CompletedHandler 事件等待）-> 解析文字块
- [x] 工厂优先 Windows 原生、失败回退 Tesseract，接口 IOcrEngine 不变
- [x] 打包 Tesseract 运行时回退（dist/tesseract/：tesseract.exe + 依赖 DLL + eng/chi_sim 语言包）
- [x] 写编译/链接验证（MinGW g++ 通过，含 -lruntimeobject -lole32）

### 任务 3.2b：空闲窗口批量补处理（未处理截屏的叠加视图）

> 用户要求：每小时前几分钟 / 截屏频率低时，后台批量处理未处理截屏（无界面，仅日志）。
> 经确认：叠加视图 = 后台批量处理（无界面）；OCR 时机 = 实时 + 空闲补处理。

- [x] 维护未处理截屏队列 `g_pending`（HBITMAP 所有权随队列，满 60 张丢最旧）
- [x] 实时路径：变化时立即 OCR 落库（主路径）
- [x] 延后入队：高频突发（距上次变化 < 4s）或实时 OCR 失败时入队，空闲窗口补处理
- [x] 空闲窗口判定 `IsIdleWindow()`：每小时前 5 分钟，或连续 60s 无变化
- [x] 后台批量 `DrainPendingBatch()`：依次识别队列截屏、落库、释放位图（无界面，仅日志）

### 任务 3.3：屏幕截取（内存中）

- [x] 用 CreateCompatibleDC + BitBlt 截取屏幕
- [x] 转为 OCR 可识别的格式
- [x] 确保截取过程不落盘（纯内存操作）

### 任务 3.4：全屏 OCR 采集

- [x] 每次变化触发时截取全屏并运行 OCR
- [x] 获取所有文字块的内容、坐标、置信度
- [x] 存储到 text_blocks 表

### 任务 3.5：Accessibility API 补充

- [x] 学习 UIA 基础 API（官方 uiautomation.h，随 Windows SDK / MSVC）
- [x] 获取当前活动窗口的控件树（uia_capture.cpp：CoCreateInstance CLSID_CUIAutomation + 递归遍历，深度≤12/节点≤2000）
- [x] 与 OCR 结果合并（controls 表按 window_id 关联；--control 可按控件名/Id/类型检索）

**阶段三完成标志：** 数据库中的文字块和屏幕上的文字一致

---

## 阶段四：变化检测引擎（第7-9周）

**工具：** VSCode + MSVC
**目标：** 只在屏幕真正变化时存储
**代码量：** ~800 行

### 任务 4.1：0.2 秒静默合并

- [x] 每次变化触发时启动 200ms 计时器（ScheduleCapture -> SetTimer(id=3,200ms)）
- [x] 200ms 内再次触发则重置计时器（多次 SetTimer 同 id 即重置）
- [x] 200ms 无新变化才执行存储（WM_TIMER id=3 -> KillTimer + TriggerCapture）

### 任务 4.2：窗口级触发（高优先级）

- [x] 监听窗口切换事件（SetWinEventHook，三组范围：CREATE..STATECHANGE / NAMECHANGE / SYSTEM_FOREGROUND）
- [x] 监听窗口标题变化（EVENT_OBJECT_NAMECHANGE）
- [x] 窗口切换时立即触发采集（事件钩子 -> ScheduleCapture -> 200ms 后 TriggerCapture）

### 任务 4.3：内容级触发（低优先级）

- [ ] 监听滚动事件
- [ ] 监听键盘输入
- [ ] 这些触发走 200ms 合并逻辑

### 任务 4.4：无效变化过滤

- [x] 过滤系统时钟刷新（NAMECHANGE 且标题整体为时钟串如 "14:30" 则忽略）
- [x] 过滤光标闪烁（刻意不订阅 EVENT_OBJECT_LOCATIONCHANGE，光标/动画每帧的噪声不触发）
- [x] 过滤任务栏通知闪烁（仅窗口对象事件且忽略自身消息窗口）
- [x] 过滤窗口 1px 抖动（1px 抖动不触发窗口级事件，且 LOCATIONCHANGE 未订阅）

### 任务 4.5：相似度去重

- [ ] 计算当前与上次文字照片的差异
- [ ] 变化 < 10% → 不存储
- [ ] 变化 10%-30% → 只存文字，不存截图
- [ ] 变化 > 30% → 存截图 + 文字
> 注：4.5 依赖"截图存储"，当前阶段一~三仅存 OCR 文字/窗口元数据/控件树，未落盘截图，故 4.5 暂未实现（待阶段七叠加视图引入截图后再做）。

**阶段四完成标志：** 运行1小时，存储次数远低于触发次数

---

## 阶段五：加密与安全（第9-11周）

**工具：** VSCode + MSVC
**目标：** 所有数据加密存储，用户密码保护
**代码量：** ~1000 行

### 任务 5.1：集成 OpenSSL

- [ ] 下载 OpenSSL 库（或使用 vcpkg 安装）
- [ ] 在项目中链接 OpenSSL
- [ ] 写测试：AES-256-GCM 加密和解密

### 任务 5.2：PBKDF2 密钥派生

- [ ] 实现 PBKDF2（迭代 600000 次，SHA-256）
- [ ] 生成随机 Salt（32 字节）
- [ ] 从用户密码 + Salt 派生 AES 密钥

### 任务 5.3：数据库加密

- [ ] 加密字段：窗口标题、应用名、文字内容
- [ ] 不加密字段：坐标、时间戳、Z序
- [ ] 读取时解密敏感字段

### 任务 5.4：密码管理

- [ ] 首次启动：用户设置通用密码（≥8位）
- [ ] 生成 Salt 并存储到配置文件
- [ ] 后续启动：用户输入密码，验证并解密
- [ ] 连续 5 次密码错误 → 锁定 5 分钟

### 任务 5.5：标签级联密钥

- [ ] 用户只设置一个主密码
- [ ] 每个标签的密钥 = PBKDF2（主密码 + 标签名 + Salt）
- [ ] 举证时只用主密码验证，只解密指定标签

**阶段五完成标志：** 数据库文件用 SQLite 工具打开看到乱码

---

## 阶段六：AI 本地部署（第11-13周）

**工具：** VSCode + MSVC + Ollama
**目标：** 用户无需实名认证即可使用 AI 功能
**代码量：** ~1000 行

### 任务 6.1：Ollama 打包与分发

- [ ] 下载 Ollama Windows 安装包
- [ ] 将 Ollama 打包进你的软件安装包
- [ ] 确保用户安装时自动安装 Ollama（静默安装）

### 任务 6.2：首次启动引导

- [ ] 检测用户硬件（GPU、显存、内存）
- [ ] 根据硬件推荐模型（Qwen2.5:7B 或更小版本）
- [ ] 引导用户下载模型（显示下载进度）
- [ ] 自动加载模型，显示"AI 已就绪"

### 任务 6.3：应用内集成

- [ ] 启动时检测 Ollama 服务是否运行
- [ ] 如未运行则自动启动
- [ ] 调用 http://localhost:11434/api/chat
- [ ] 支持自动分类：新记录写入时调用 AI 判断标签
- [ ] 支持语义搜索：用户查询时调用 AI 理解意图

### 任务 6.4：硬件不达标的降级方案

- [ ] 检测到硬件不达标时（无独显、内存<8GB）
- [ ] 降级为轻量关键词匹配（FTS5 + BM25）
- [ ] 或引导用户使用云端 API（需实名，可选）

**阶段六完成标志：** 用户在软件里输入问题，能收到 AI 回答

---

## 阶段七：搜索与展示（第13-16周）

**工具：** VSCode + MSVC
**目标：** 用户能搜索历史记录，查看时间轴和叠加视图
**代码量：** ~2500 行

### 任务 7.1：FTS5 全文索引

- [ ] 在 SQLite 中创建 FTS5 虚拟表
- [ ] 将文字内容写入 FTS5 表
- [ ] 用 MATCH 语法执行搜索

### 任务 7.2：BM25 相关性排序

- [ ] 使用 SQLite FTS5 的 bm25 函数
- [ ] 按 BM25 分数从高到低排序
- [ ] 显示每条结果的置信度分数

### 任务 7.3：多维匹配展示

- [ ] 计算时间匹配度
- [ ] 计算内容匹配度（BM25 归一化）
- [ ] 综合置信度展示

### 任务 7.4：全局搜索入口

- [ ] 注册全局热键 Ctrl+Shift+Space
- [ ] 按下热键弹出搜索框
- [ ] 输入关键词实时显示搜索结果

### 任务 7.5：时间轴视图

- [ ] 显示横向时间轴
- [ ] 每个变化节点显示为点
- [ ] 支持拖拽缩放

### 任务 7.6：叠加视图渲染

- [ ] 从数据库读取窗口布局元数据
- [ ] 按 Z 序从底到顶绘制每个窗口
- [ ] 在窗口区域内渲染文字照片

**阶段七完成标志：** 按 Ctrl+Shift+Space 搜索"合同"能看到相关记录

---

## 阶段八：打包与分发（第16-18周）

**目标：** 用户能双击安装，3 分钟内开始用
**代码量：** ~500 行

### 任务 8.1：MSIX 打包

- [ ] 安装 Windows App SDK
- [ ] 创建 MSIX 项目
- [ ] 配置应用名称、图标、版本号
- [ ] 生成 MSIX 安装包

### 任务 8.2：Microsoft Store 上架

- [ ] 注册微软合作伙伴中心账号（个人开发者免费）
- [ ] 提交应用详情页（名称、描述、截图）
- [ ] 提交 MSIX 包
- [ ] 等待审核（1-3天）

### 任务 8.3：应用详情页内容

- [ ] 应用名称：YO-Trace
- [ ] 描述："记录屏幕变化，帮你找回记忆"
- [ ] 截图：至少 3 张
- [ ] 存储说明："3个月数据约40GB，建议存非系统盘"

### 任务 8.4：VSCode 插件

- [ ] 创建 VSCode 插件项目
- [ ] 实现核心功能：记录文件切换
- [ ] 提供搜索入口：侧边栏 + 快捷键
- [ ] 发布到 VSCode 插件市场

### 任务 8.5：用户测试

- [ ] 找 5 个非技术背景的人安装试用
- [ ] 记录安装过程是否顺畅
- [ ] 修复最严重的 3 个问题

**阶段八完成标志：** 非技术用户从下载到搜索出结果 < 3 分钟

---

## 里程碑总览

| 里程碑     | 完成标志        | 预计时间  | 代码量    |
| ------- | ----------- | ----- | ------ |
| M0：验证方向 | 确定需求真实存在    | 第1周末  | 0行     |
| M1：窗口采集 | 能枚举所有窗口     | 第3周末  | ~500行  |
| M2：本地存储 | 数据库中有窗口记录   | 第4周末  | ~800行  |
| M3：文字识别 | 能识别屏幕文字并存储  | 第7周末  | ~1500行 |
| M4：变化检测 | 存储量降低80%以上  | 第9周末  | ~800行  |
| M5：加密存储 | 数据库文件查看为乱码  | 第11周末 | ~1000行 |
| M6：AI部署 | 用户能用上本地AI   | 第13周末 | ~1000行 |
| M7：搜索展示 | 能搜出历史记录+时间轴 | 第16周末 | ~2500行 |
| M8：打包分发 | 非技术用户能用上    | 第18周末 | ~500行  |

**总计代码量：** ~8600 行
**总预计时间：** 18 周（碎片化时间可拉长至 6-12 个月）

---

## 即刻开始

> [!TODO]
> 打开浏览器，访问 https://github.com/windingwind/windrecorder
>
> 读完 README
> 
> 写下 3 个 Windrecorder 让你想骂人的地方
> 
> 明天开始阶段零的任务 0.2

**今天不用写任何代码。**

---

*文档版本: V2.0 | 更新日期: 2026年8月26日 | 作者: YO-Trace Team*
